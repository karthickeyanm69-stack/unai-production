import React from 'react';

// Single Navigation Bar Standard: TopHeader returns null so only the BottomNavDock exists as the single navbar.
export const TopHeader: React.FC = () => {
  return null;
};
